import { testUsingGenerator } from './test-using-generator';

describe('testUsingGenerator', () => {
  it('should work', () => {
    expect(testUsingGenerator()).toEqual('test-using-generator');
  });
});
